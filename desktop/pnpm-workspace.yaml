# pnpm 11 reads build-script approvals from here (not from package.json).
# Without this, pnpm blocks Electron's postinstall and the binary never downloads.
allowBuilds:
  electron: true
  electron-builder: true
  esbuild: true
