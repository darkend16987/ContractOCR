# OCR Engines
# NOTE: Requires Python 3.12 (NOT 3.13 — vietocr's pinned deps have no cp313 wheels).
#
# Default engine = RapidViet (RapidOCR ONNX detection + VietOCR recognition):
# fast detection with no paddlepaddle, plus correct Vietnamese diacritics. This is
# the only path the shipped build bundles (see sidecar.spec).
rapidocr>=3.0.0
onnxruntime>=1.20.0
# torch is needed by VietOCR but is NOT pulled in automatically, so list it here.
vietocr>=0.3.5
torch>=2.6.0
torchvision>=0.21.0

# OPTIONAL fallback engines (OCR_ENGINE=paddleocr/hybrid). NOT installed by default
# and NOT bundled in the desktop build — RapidViet supersedes them. Install only if
# you specifically need the paddle pipeline:  pip install paddleocr==3.7.0 paddlepaddle==3.3.1
# (On Windows torch must import before paddle — handled in src/ocr/engine.py.)
# paddleocr==3.7.0
# paddlepaddle==3.3.1

# Image processing
Pillow>=10.0.0
pdf2image>=1.16.0
opencv-python-headless==4.10.0.84  # 4.10 = numpy-2 ABI compatible; matches paddlex's opencv pin

# PDF (sidecar): searchable-PDF text layer in /searchable.
# Font for the invisible text layer = DejaVuSans.ttf from matplotlib (a transitive
# dep via paddlex/imgaug). P5 packaging must bundle this TTF explicitly.
pymupdf>=1.24.0

# AI Agent - Gemini (new unified SDK)
google-genai>=1.0.0

# Output formats
openpyxl>=3.1.0          # xlsx (extracted fields + PDF→Office tables)
python-docx>=1.1.0       # docx (PDF→Office full-text + tables)
gspread>=5.12.0
google-auth>=2.23.0

# Web UI
streamlit>=1.28.0

# API Server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.5.0
